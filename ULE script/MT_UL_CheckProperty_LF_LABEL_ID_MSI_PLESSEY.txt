#<Start the script>
{
	if(OUT1.DataType == "MSIPlessey")
	{
		labelID=GetProperty(LF_LABEL_ID_MSI_PLESSY)
		OUT1.Data= "LabelID = " + labelID + "Data = " + OUT1.Data + " Type = " + OUT1.DataType
		DoSLF(OUT1)
	}
}#<End the script>
