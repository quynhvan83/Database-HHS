#<Start the script>
{
	if(OUT1.DataType == "Plessey")
	{
		labelID=GetProperty(LF_LABEL_ID_ PLESSEY)
		OUT1.Data= "LabelID = " + labelID + "Data = " + OUT1.Data + " Type = " + OUT1.DataType
		DoSLF(OUT1)
	}
}#<End the script>
