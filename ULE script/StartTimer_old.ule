# Use for test functional of StartTimer
{
	if(OUT1.DataType=="Code39")
	{
		OUT1.Data="Start Timer 5s\n"
		StartTimer(OUT1,50)
	}
	if(OUT1.DataType=="TimerExpired")
	{
		OUT1.Data="5s Expired"
		OUT1.DataType="Code128"
		ULEBeep(1,3,2,0,50,50)

	}
}

