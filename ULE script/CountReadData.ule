{	
	if (IsInMemory("INT1") == 0)
	{
		SaveInt(1, "INT1")
		exit
	}
	else
	{
		count = RestoreInt("INT1")
		count = count + 1
		OUT1.Data = "Test: " + IntToStr(count) + "\n"
		ReplaceInt(count, "INT1")
		OUT2 = OUT1
		OUT2.Data = "Count: " + IntToStr(count) + "\n"
		
		if (count == 9)
		{
			OUT1.Data = "Finish\n"
			FreeMemory()
		}
		
	}	
}